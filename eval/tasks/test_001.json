{
  "id": "test_001",
  "sub_eval": "test",
  "task_prompt": "Add a `#[test]` block at the bottom of this file that thoroughly tests the `is_palindrome` function. The test must pass when run with `cargo test`. Include cases for: empty string, single character, simple palindrome, non-palindrome, palindrome with mixed case (treat case-sensitively as the implementation does). Return the complete program (existing code + new tests) in a single ```rust code block.",
  "input_code": "pub fn is_palindrome(s: &str) -> bool {\n    let chars: Vec<char> = s.chars().collect();\n    let n = chars.len();\n    for i in 0..n / 2 {\n        if chars[i] != chars[n - 1 - i] {\n            return false;\n        }\n    }\n    true\n}\n\nfn main() {\n    println!(\"{}\", is_palindrome(\"racecar\"));\n}\n",
  "verifier_cmd": "cargo test",
  "expected_outcome": "tests_pass",
  "difficulty": "easy",
  "source": "hand-curated by Claude Code",
  "notes": "Tests must compile AND pass. Note: implementation is case-sensitive, so 'Racecar' is NOT a palindrome under it."
}
